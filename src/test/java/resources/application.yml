spring:
#  datasource:
#    url: jdbc:h2:mem:test
#    username: sa
#    password:
#    driver-class-name: org.h2.Driver
#
#  jpa:
#    hibernate:
#      ddl-auto: create
#    properties:
#      hibernate:
#        #show_sql: true
#        format_sql: true
# 스프링부트에서는 테스트 코드 돌리면 기본적으로 테스트를 메모리로 돌리기 때문에 위 설정을 해줄 필요가 없다.
logging:
  level:
   org.hibernate.SQL: debug
   org.hibernate.type: trace